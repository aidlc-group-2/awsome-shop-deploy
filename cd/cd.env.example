# CD 配置模板 — 复制为 cd/cd.env 使用（真实 cd.env 不入库）
# 全部可选：不配置时 CD 也能跑（匿名 API + 无群通知）。

# GitHub 只读 token（fine-grained PAT，仅 public repo 读取权限即可）
# 作用：把 Checks API 配额从 60 次/h/IP 提到 5000 次/h，避免轮询限流
#GITHUB_TOKEN=github_pat_xxx

# 群机器人 webhook（飞书/Slack incoming webhook）
# 作用：部署结果 + 构建失败日志尾部 + 冒烟摘要自动进群
#CD_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxx

# 是否允许部署"没接 CI 的仓库"（默认 false：未接 CI 一律 SKIP，推动接入门禁）
# 过渡期可临时设 true，全员接入 ci-setup-guide 后务必关掉
#ALLOW_NO_CI=false
